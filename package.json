{
  "name": "agentforge",
  "private": true,
  "scripts": {
    "dev": "concurrently -n frontend,backend -c cyan,magenta \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run start:dev",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "npm --prefix frontend run build",
    "build:backend": "npm --prefix backend run build",
    "db:up": "docker-compose -f backend/docker-compose.yml up -d",
    "db:down": "docker-compose -f backend/docker-compose.yml down",
    "db:migrate": "npm --prefix backend run prisma:migrate",
    "db:studio": "npm --prefix backend exec prisma studio",
    "setup": "npm run db:up && sleep 2 && npm run db:migrate && echo 'Ready! Run: npm run dev'"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
